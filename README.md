1️⃣ What is the difference between var, let, and const?
=>VAR is a function scoped.it is accessable in the which it is declared.It can re declared
LET is a block scooped ,it is only accesable where it is declared,and it is can be updated but it cant re daclared.
const it is also block scooped but it cant be update or redecalared.
2️⃣ What is the spread operator (...)?
=>THis featured used to expand or unpack elements of an array, object, or iterable into individual elements.with the help this we can copy of an array,combines multiple arrays into one,It copies properties of an object.
3️⃣ What is the difference between map(), filter(), and forEach()?
=>Map=It is used to transform each element of an array.
Returns a new array with modified values.
Filter=Used to select elements based on a condition.
returns the array with only those who satisfied the logic.
Foreach=It is used to iterate over elements for performing actions.
it is used for updating values.
4️⃣ What is an arrow function?
=>It is a shorter way to write a function javascript ,which is updated in ES6
5️⃣ What are template literals?
=>Template literals is a feature in ES6 that allow to create strings using backticks ( ). They make it easier to include variables,objects inside strings.
